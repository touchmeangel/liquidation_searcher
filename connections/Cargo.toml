[package]
name = "connections"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0.100"
redis = { version = "1.0.2", features = ["tokio-comp", "connection-manager"] }
solana-pubkey = "^2"
